<%= javascript_tag do %>
  var width = window.screen.availWidth;
  var width = window.screen.availHeight;
<% end %>

<div style='width: 1440px;'>
  <div id="map" style='width: 1440px; height: 700px;'></div>
</div>

<%= latitude %>
,
<%= longitude %>
<br>
<%= user_ip %>
<br>
<%= Geocoder.search("193.2.176.10").first.latitude %>-
<%= Geocoder.search("193.2.176.10").first.longitude %>

<%= javascript_tag do %>
	handler = Gmaps.build('Google');
  handler.buildMap(
    {internal: {id: 'map'}},
    function(){
      handler.map.centerOn([<%= Geocoder.search("193.2.176.10").first.latitude %>, <%= Geocoder.search("193.2.176.10").first.longitude %>]);
      handler.getMap().setZoom(15);

      handler.addMarkers(<%=raw @hash.to_json %>);

      //handler.bounds.extendWith(markers);
	    handler.fitMapToBounds();
    });

<% end %>
